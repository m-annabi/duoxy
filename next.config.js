/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true, // Default, can be kept or removed
  eslint: {
    ignoreDuringBuilds: true,
  },
};

module.exports = nextConfig;
